@page "/"
@using REVOPS.DevChallenge.Context.Entities
@using REVOPS.DevChallenge.Services
@inject IChatInfoService _chatInfoService

<PageTitle>RevOps Chat Info</PageTitle>

<div class="main-container">
    <!-- Cabeçalho -->
    <div class="page-header">
        <h1 style="color: #005C55 !important; -webkit-text-fill-color: #005C55 !important;">Informações do Talk</h1>
        <p style="color: #4a5568;">Inserir o ID da conversa do Umblerito no campo de pesquisa abaixo para reter informações sobre a conversa e contato.</p>
    </div>

    <!-- Seção Ativa -->
    <div class="active-section mb-5">
        <!-- Formulário de Busca -->
        <div class="card mb-4 search-card shadow-glow">
            <div class="card-body">
                <form @onsubmit="GetChatInfo">
                    <div class="search-section">
                        <input type="text" 
                               class="form-control" 
                               placeholder="Digite o ID do chat (ex: aPZ-CitJaYbmlVCa)"
                               @bind="chatId" 
                               @bind:event="oninput" />
                        <button type="submit" class="btn" style="background: linear-gradient(135deg, #005C55 0%, #17A5DE 100%); color: white;" disabled="@isLoading">
                            @if (isLoading)
                            {
                                <span class="spinner-border spinner-border-sm me-2" role="status"></span>
                                <span>Buscando...</span>
                            }
                            else
                            {
                                <i class="bi bi-search me-2"></i><span>Buscar Chat</span>
                            }
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Mensagens de Erro / Não Encontrado -->
        @if (somethingBadHappened)
        {
            <div class="alert alert-danger fade-in mb-4">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                <strong>Erro:</strong> @error
            </div>
        }

        @if (chatNotFound)
        {
            <div class="alert alert-warning fade-in mb-4">
                <i class="bi bi-search me-2"></i>
                <strong>Chat não encontrado:</strong> Verifique se o ID está correto.
            </div>
        }

        <!-- Display do Chat Ativo -->
        @if (currentChatInfo != null)
        {
            <div class="active-chat-container fade-in">
                <ChatInfoCard Record="currentChatInfo" />
            </div>
        }
    </div>

    <!-- Seção de Histórico - Tabela em Largura Total -->
    <div class="history-section pt-4 border-top border-secondary">
        <ChatHistoryPanel History="searchHistory" OnSelect="SelectFromHistory" />
    </div>
</div>

<style>
    .search-card {
        max-width: 800px;
        margin-left: auto;
        margin-right: auto;
    }
    .active-chat-container {
        max-width: 900px;
        margin-left: auto;
        margin-right: auto;
    }
    .main-container {
        padding-bottom: 5rem;
    }
</style>

@code {
    private string? chatId = null;
    private ChatInfoRecord? currentChatInfo = null;
    private List<ChatInfoRecord> searchHistory = new();
    private bool somethingBadHappened = false;
    private bool chatNotFound = false;
    private bool isLoading = false;
    private string? error = null;

    protected override async Task OnInitializedAsync()
    {
        await LoadHistory();
    }

    private async Task LoadHistory()
    {
        try
        {
            searchHistory = await _chatInfoService.GetHistoryAsync(50);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Erro ao carregar histórico: {ex.Message}");
        }
    }

    private async Task GetChatInfo()
    {
        if (string.IsNullOrWhiteSpace(chatId))
            return;

        isLoading = true;
        chatNotFound = false;
        somethingBadHappened = false;
        error = null;
        currentChatInfo = null;

        try
        {
            var result = await _chatInfoService.GetChatInfoAsync(chatId.Trim());

            if (result != null)
            {
                currentChatInfo = result;
                await LoadHistory(); // Atualiza histórico
            }
            else
            {
                chatNotFound = true;
            }
        }
        catch (Exception e)
        {
            somethingBadHappened = true;
            error = e.Message;
        }
        finally
        {
            isLoading = false;
        }
    }

    private void SelectFromHistory(ChatInfoRecord record)
    {
        currentChatInfo = record;
        chatId = record.ChatId;
        chatNotFound = false;
        somethingBadHappened = false;
        error = null;
        
        // Rolar para o topo
        // Em um app real usaríamos JS interop, mas por enquanto o usuário apenas vê a atualização
    }
}
